{
  "name": "aws-devops-monitor",
  "version": "1.1.0",
  "description": "System health dashboard for AWS project",
  "main": "app.js",
  "scripts": {
    "start": "node app.js",
    "test": "echo \"Error: no test specified\" && exit 0"
  },
  "engines": {
    "node": ">=16.0.0"
  }
}